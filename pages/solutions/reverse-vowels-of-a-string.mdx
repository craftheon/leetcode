import { Problem, Code, Solution, Similar } from '@/components'

<Problem slug="reverse-vowels-of-a-string" id="345" title="Reverse Vowels of a String" difficulty="Easy" tags={[{"name":"Two Pointers","slug":"two-pointers"},{"name":"String","slug":"string"}]} >
    Given a string `s`, reverse only all the vowels in the string and return it.

The vowels are `'a'`, `'e'`, `'i'`, `'o'`, and `'u'`, and they can appear in both lower and upper cases, more than once.

**Example 1:**

```javascript 
Input: s = "hello"
Output: "holle"

```

**Example 2:**

```javascript 
Input: s = "leetcode"
Output: "leotcede"

```

**Constraints:**

*   `1 <= s.length <= 3 * 105`
*   `s` consist of **printable ASCII** characters.
</Problem >

<Solution>
write solution in here
</Solution>

<Code>
```javascript
// write code in here
```
</Code>

<Similar data={[{"title":"Reverse String","slug":"reverse-string","difficulty":"Easy"},{"title":"Remove Vowels from a String","slug":"remove-vowels-from-a-string","difficulty":"Easy"},{"title":"Faulty Keyboard","slug":"faulty-keyboard","difficulty":"Easy"},{"title":"Sort Vowels in a String","slug":"sort-vowels-in-a-string","difficulty":"Medium"}]} />
