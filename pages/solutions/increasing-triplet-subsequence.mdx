import { Problem, Code, Solution, Similar } from '@/components'

<Problem slug="increasing-triplet-subsequence" id="334" title="Increasing Triplet Subsequence" difficulty="Medium" tags={[{"name":"Array","slug":"array"},{"name":"Greedy","slug":"greedy"}]} >
    Given an integer array `nums`, return `true` _if there exists a triple of indices_ `(i, j, k)` _such that_ `i < j < k` _and_ `nums[i] < nums[j] < nums[k]`. If no such indices exists, return `false`.

**Example 1:**

```javascript 
Input: nums = [1,2,3,4,5]
Output: true
Explanation: Any triplet where i < j < k is valid.
```

**Example 2:**

```javascript 
Input: nums = [5,4,3,2,1]
Output: false
Explanation: No triplet exists.
```

**Example 3:**

```javascript 
Input: nums = [2,1,5,0,4,6]
Output: true
Explanation: The triplet (3, 4, 5) is valid because nums[3] == 0 < nums[4] == 4 < nums[5] == 6.
```

**Constraints:**

*   `1 <= nums.length <= 5 * 105`
*   `-231 <= nums[i] <= 231 - 1`

**Follow up:** Could you implement a solution that runs in `O(n)` time complexity and `O(1)` space complexity?
</Problem >

<Solution>
write solution in here
</Solution>

<Code>
```javascript
// write code in here
```
</Code>

<Similar data={[{"title":"Longest Increasing Subsequence","slug":"longest-increasing-subsequence","difficulty":"Medium"},{"title":"Count Special Quadruplets","slug":"count-special-quadruplets","difficulty":"Easy"},{"title":"Count Good Triplets in an Array","slug":"count-good-triplets-in-an-array","difficulty":"Hard"},{"title":"Count Increasing Quadruplets","slug":"count-increasing-quadruplets","difficulty":"Hard"}]} />
