import { Problem, Code, Solution, Similar } from '@/components'

<Problem slug="remove-duplicates-from-sorted-list" id="83" title="Remove Duplicates from Sorted List" difficulty="Easy" tags={[{"name":"Linked List","slug":"linked-list"}]} >
    Given the `head` of a sorted linked list, _delete all duplicates such that each element appears only once_. Return _the linked list **sorted** as well_.

**Example 1:**

![](https://assets.leetcode.com/uploads/2021/01/04/list1.jpg)
```javascript 
Input: head = [1,1,2]
Output: [1,2]
```

**Example 2:**

![](https://assets.leetcode.com/uploads/2021/01/04/list2.jpg)
```javascript 
Input: head = [1,1,2,3,3]
Output: [1,2,3]
```

**Constraints:**

*   The number of nodes in the list is in the range `[0, 300]`.
*   `-100 <= Node.val <= 100`
*   The list is guaranteed to be **sorted** in ascending order.
</Problem >

<Solution>
write solution in here
</Solution>

<Code>
```javascript
// write code in here
```
</Code>

<Similar data={[{"title":"Remove Duplicates from Sorted List II","slug":"remove-duplicates-from-sorted-list-ii","difficulty":"Medium"},{"title":"Remove Duplicates From an Unsorted Linked List","slug":"remove-duplicates-from-an-unsorted-linked-list","difficulty":"Medium"}]} />
