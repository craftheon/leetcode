import { Problem, Code, Solution, Similar } from '@/components'

<Problem slug="backspace-string-compare" id="874" title="Backspace String Compare" difficulty="Easy" tags={[{"name":"Two Pointers","slug":"two-pointers"},{"name":"String","slug":"string"},{"name":"Stack","slug":"stack"},{"name":"Simulation","slug":"simulation"}]} >
    Given two strings `s` and `t`, return `true` _if they are equal when both are typed into empty text editors_. `'#'` means a backspace character.

Note that after backspacing an empty text, the text will continue empty.

**Example 1:**

```javascript 
Input: s = "ab#c", t = "ad#c"
Output: true
Explanation: Both s and t become "ac".

```

**Example 2:**

```javascript 
Input: s = "ab##", t = "c#d#"
Output: true
Explanation: Both s and t become "".

```

**Example 3:**

```javascript 
Input: s = "a#c", t = "b"
Output: false
Explanation: s becomes "c" while t becomes "b".

```

**Constraints:**

*   `1 <= s.length, t.length <= 200`
*   `s` and `t` only contain lowercase letters and `'#'` characters.

**Follow up:** Can you solve it in `O(n)` time and `O(1)` space?
</Problem >

<Solution>
write solution in here
</Solution>

<Code>
```javascript
/**
 * @param {string} s
 * @param {string} t
 * @return {boolean}
 */
var backspaceCompare = function(s, t) {
    let ss = [], ts = []
    for(let i = 0; i < s.length; i ++) {
      s[i] !== "#" ? ss.push(s[i]) : ss.pop()
    }
    for(let i = 0; i < t.length; i ++) {
      t[i] !== "#" ? ts.push(t[i]) : ts.pop()
    }
    if (ss.length !== ts.length) return false
    for(let i = 0; i < ss.length; i ++) {
      if (ss[i] !== ts[i]) return false
    }
    return true
};
```
</Code>

<Similar data={[{"title":"Crawler Log Folder","slug":"crawler-log-folder","difficulty":"Easy"},{"title":"Removing Stars From a String","slug":"removing-stars-from-a-string","difficulty":"Medium"}]} />
