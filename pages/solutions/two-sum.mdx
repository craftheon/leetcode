import { Problem, Code, Solution } from '@/components'

<Problem slug="two-sum" title="Two Sum" difficulty="" tags={[]}>

</Problem>

<Solution>

</Solution>

<Code>
```javascript
function twoSum(nums, target) {
  const map = {}
  for (const [index, num] of nums.entries()) {
    if (map[target - num] !== undefined) {
      return [map[target - num], index]
    }
    map[num] = index
  }
  return []
}
```
</Code>

