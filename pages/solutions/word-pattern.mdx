import { Problem, Code, Solution, Similar } from '@/components'

<Problem slug="word-pattern" id="290" title="Word Pattern" difficulty="Easy" tags={[{"name":"Hash Table","slug":"hash-table"},{"name":"String","slug":"string"}]} >
    Given a `pattern` and a string `s`, find if `s` follows the same pattern.

Here **follow** means a full match, such that there is a bijection between a letter in `pattern` and a **non-empty** word in `s`.

**Example 1:**

```javascript 
Input: pattern = "abba", s = "dog cat cat dog"
Output: true

```

**Example 2:**

```javascript 
Input: pattern = "abba", s = "dog cat cat fish"
Output: false

```

**Example 3:**

```javascript 
Input: pattern = "aaaa", s = "dog cat cat dog"
Output: false

```

**Constraints:**

*   `1 <= pattern.length <= 300`
*   `pattern` contains only lower-case English letters.
*   `1 <= s.length <= 3000`
*   `s` contains only lowercase English letters and spaces `' '`.
*   `s` **does not contain** any leading or trailing spaces.
*   All the words in `s` are separated by a **single space**.
</Problem >

<Solution>
write solution in here
</Solution>

<Code>
```javascript
// write code in here
```
</Code>

<Similar data={[{"title":"Isomorphic Strings","slug":"isomorphic-strings","difficulty":"Easy"},{"title":"Word Pattern II","slug":"word-pattern-ii","difficulty":"Medium"}]} />
